@model SMS.Models.mst_section

@{
    ViewBag.Title = "DeleteSection";
}
<div class="right_col" role="main">
    <h2>Delete Section</h2>

    <h3>Are you sure you want to delete this?</h3>
    <div>
        <hr />
        <dl class="dl-horizontal">
           
            <dd>
                @Html.DisplayFor(model => model.class_id)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.class_name)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.class_name)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.Section_name)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Section_name)
            </dd>

        </dl>

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="form-actions no-color">
                <input type="submit" value="Delete" class="btn btn-default" /> |
                @Html.ActionLink("Back to List", "AllSectionList")
            </div>
        }
    </div>
    </div>