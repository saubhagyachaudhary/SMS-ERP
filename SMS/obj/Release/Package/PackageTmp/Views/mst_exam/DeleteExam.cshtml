@model SMS.Models.mst_exam

@{
    ViewBag.Title = "DeleteExam";
}

<div class="right_col" role="main">
    <div class="row">
        <div class="col-md-12 col-sm-12 col-xs-12">
            <div class="x_panel">
                <h2>Delete Subject</h2>

                <h3>Are you sure you want to delete this?</h3>
                <div>
                    <hr />
                    <dl class="dl-horizontal">
                        <dt>
                            @Html.DisplayNameFor(model => model.exam_name)
                        </dt>

                        <dd>
                            @Html.DisplayFor(model => model.exam_name)
                        </dd>

                    </dl>

                    @using (Html.BeginForm())
                    {
                        @Html.AntiForgeryToken()

                        <div class="form-actions no-color">
                            <input type="submit" value="Delete" class="btn btn-default please_wait" />
                            @Html.ActionLink("Back to List", "AllExamList", new { area = "" }, new { @class = "btn btn-primary please_wait" })
                        </div>
                    }
                </div>
            </div>
            </div>
</div>
</div>